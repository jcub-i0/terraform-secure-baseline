# tf-secure-baseline Validation Report Template

## Purpose

This template provides a client-facing or internal handoff format for documenting validation results from one generated `tf-secure-baseline` validation evidence package.

Use this template for one of the four validation scopes produced by the project:

```text
workload-bootstrap
workload-baseline
control-plane
security-operations
```

This template is intended to summarize:

- which validation scope was reviewed
- when validation was performed
- which export script generated the evidence package
- which automated validation checks passed or failed
- which evidence files were generated
- which checks remain outside the selected report
- any warnings, exceptions, or limitations

The project generates separate evidence packages for each validation layer. A single combined-report exporter is not currently part of the baseline.

---

## Generated Evidence Package

| Field | Value |
|---|---|
| Client / Organization | `<client-name>` |
| Project / Engagement | `<project-name>` |
| Prepared By | `<name>` |
| Prepared Date | `<YYYY-MM-DD>` |
| Report Version | `<version>` |
| Validation Scope | `<workload-bootstrap/workload-baseline/control-plane/security-operations>` |
| Generated By Script | `<export-bootstrap.sh/export-baseline.sh/export-control-plane.sh/export-security-operations.sh>` |
| Report Package Location | `<validation-results/...>` |
| Validation Time | `<timestamp>` |

Expected generated evidence package locations:

| Validation Scope | Export Script | Expected Location |
|---|---|---|
| Workload bootstrap | `scripts/validation/export-bootstrap.sh <env>` | `validation-results/<env>/bootstrap/<timestamp>/` |
| Workload baseline | `scripts/validation/export-baseline.sh <env>` | `validation-results/<env>/baseline/<timestamp>/` |
| Control plane | `scripts/validation/export-control-plane.sh` | `validation-results/control-plane/<timestamp>/` |
| Security operations | `scripts/validation/export-security-operations.sh` | `validation-results/security-operations/security-services/<timestamp>/` |

---

## Environment / Account Metadata

Complete the fields that apply to the selected validation scope.

| Field | Value |
|---|---|
| Cloud Name | `<cloud-name>` |
| Name Prefix | `<name-prefix>` |
| Validation Layer | `<Workload Bootstrap/Workload Baseline/Control Plane/Security Operations>` |
| Workload Environment | `<dev/staging/prod/not-applicable>` |
| Control-Plane Environment | `<control-plane/not-applicable>` |
| Security-Operations Environment | `<security-operations/not-applicable>` |
| AWS Profile | `<profile-or-not-set>` |
| AWS Credential Source | `<AWS CLI profile/GitHub OIDC environment credentials/AWS default credential chain>` |
| AWS Account ID | `<account-id>` |
| Expected Account ID | `<expected-account-id/not-set>` |
| AWS Region | `<region>` |
| Expected GitHub Repository | `<owner/repo/not-set/not-applicable>` |

Environment-specific scripts derive `NAME_PREFIX` from `CLOUD_NAME` and the environment name unless `NAME_PREFIX` is explicitly overridden:

```bash
CLOUD_NAME="${CLOUD_NAME:-tf-secure-baseline}"
NAME_PREFIX="${NAME_PREFIX:-${CLOUD_NAME}-${ENV_NAME}}"
```

Control-plane export uses `control-plane` as its environment suffix. Security-operations export uses `security-operations`:

```bash
CONTROL_PLANE_ENV_NAME="${CONTROL_PLANE_ENV_NAME:-control-plane}"
SECURITY_OPERATIONS_ENV_NAME="${SECURITY_OPERATIONS_ENV_NAME:-security-operations}"
```

Use `not-applicable` for fields that do not apply to the selected validation scope. In GitHub Actions evidence, `AWS Profile` may be `not set`; confirm that `AWS Credential Source` is `GitHub OIDC environment credentials`.

---

## Executive Summary

| Field | Value |
|---|---|
| Overall Result | `<PASS/FAIL>` |
| Validation Scripts Passed | `<passed>/<total>` |
| Validation Scripts Failed | `<failed>/<total>` |
| Manual Validation Remaining | `<yes/no>` |

Summary:

`<Briefly summarize the validation outcome for the selected evidence package. Example: The dev workload bootstrap evidence package passed automated read-only validation with 1 of 1 validation scripts passing. Strict workload CMK policy checks were enabled, and no workload bootstrap validation failures were reported. Manual validation remains for Terraform plan/apply/destroy workflow execution, end-user access, live Lambda workflow tests, tamper testing, break-glass testing, and destroy safety review.>`

---

## Workload Bootstrap Validation Results

Use this section only when the validation scope is `workload-bootstrap`.

Generated by:

```bash
AWS_PROFILE="<env>" \
AWS_REGION="<region>" \
EXPECTED_ACCOUNT_ID="<WORKLOAD-ACCOUNT-ID>" \
EXPECTED_GITHUB_REPOSITORY="<GITHUB-OWNER>/<GITHUB-REPO>" \
REQUIRE_STATE_STACK_REMOTE=true \
CLOUD_NAME="<cloud-name>" \
./scripts/validation/export-bootstrap.sh <dev|staging|prod>
```

Expected generated package:

```text
validation-results/<env>/bootstrap/<timestamp>/
├── summary.md
├── summary.json
└── validate-bootstrap.log
```

### Workload Bootstrap Summary

| Area | Script | Result | Evidence Log | Notes |
|---|---|---|---|---|
| Workload Bootstrap | `validate-bootstrap.sh` | `<PASS/FAIL/Not Run>` | `validate-bootstrap.log` | `<notes>` |

Expected successful workload bootstrap summary:

```text
Validation scripts passed:  1/1
Validation scripts failed:  0/1
```

### Workload Bootstrap Settings

| Setting | Value |
|---|---|
| `REQUIRE_BOOTSTRAP_GITHUB_OIDC` | `<true/false>` |
| `REQUIRE_BOOTSTRAP_GITHUB_APPLY_ROLE` | `<true/false>` |
| `STRICT_WORKLOAD_CMK_POLICY_CHECKS` | `<true/false>` |
| `REQUIRE_STATE_STACK_REMOTE` | `<true/false>` |
| `STRICT_GITHUB_SUBJECT_CHECKS` | `<true/false>` |
| `EXPECTED_GITHUB_PLAN_SUBJECT` | `<value/derived/not-set>` |
| `EXPECTED_GITHUB_APPLY_SUBJECT` | `<value/derived/not-set>` |

### Strict Workload CMK Policy Checks

| Field | Value |
|---|---|
| `STRICT_WORKLOAD_CMK_POLICY_CHECKS` | `<true/false>` |
| `lambda_cmk_arn` output present | `<yes/no/not-reviewed>` |
| `secrets_manager_cmk_arn` output present | `<yes/no/not-reviewed>` |
| GitHub Apply role references current Lambda CMK ARN | `<PASS/WARN/FAIL/Not Reviewed>` |
| GitHub Apply role references current Secrets Manager CMK ARN | `<PASS/WARN/FAIL/Not Reviewed>` |

When `STRICT_WORKLOAD_CMK_POLICY_CHECKS=true`, stale or missing workload Lambda / Secrets Manager CMK policy references should be treated as validation failures.

When `STRICT_WORKLOAD_CMK_POLICY_CHECKS=false`, stale or missing workload CMK policy references are advisory warnings. This setting should be documented as an exception when used for client-facing evidence.

### Remote State Stack Validation

| Field | Value |
|---|---|
| `REQUIRE_STATE_STACK_REMOTE` | `<true/false>` |
| State stack declares an S3 backend | `<PASS/WARN/FAIL/Not Reviewed>` |
| State stack uses `use_lockfile = true` | `<PASS/WARN/FAIL/Not Reviewed>` |
| State object key is distinct from account and workload keys | `<PASS/WARN/FAIL/Not Reviewed>` |
| Remote S3 state object exists and is readable | `<PASS/WARN/FAIL/Not Reviewed>` |
| `terraform state pull` succeeds through the configured backend | `<PASS/WARN/FAIL/Not Reviewed>` |
| Backend bucket matches the state stack output | `<PASS/WARN/FAIL/Not Reviewed>` |

Direct script and exporter runs default `REQUIRE_STATE_STACK_REMOTE` to `false`, which makes remote-state findings advisory. The GitHub evidence workflows default the setting to `true`. Use `true` for v1.4.0 release validation and client-facing evidence after state migration is complete.

### Workload Bootstrap Coverage

| Area | Result | Evidence Log | Notes |
|---|---|---|---|
| Workload bootstrap directory structure | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State stack remote S3 backend migration and readability | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State, account, and workload backend files with distinct object keys | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| S3 native lockfile configuration with `use_lockfile = true` | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State bucket security | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State bucket versioning | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State bucket public access block | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State bucket SSE-KMS encryption | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| State CMK status | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| Workload GitHub OIDC provider | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| Workload GitHub Plan role | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| Workload GitHub Apply role | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub repository trust conditions | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub environment subject conditions | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub state bucket access | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub `.tflock` object access | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub state CMK access | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |
| GitHub workload CMK access | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-bootstrap.log` | `<notes>` |

---

## Workload Baseline Validation Results

Use this section only when the validation scope is `workload-baseline`.

Generated by:

```bash
AWS_PROFILE="<env>" \
AWS_REGION="<region>" \
EXPECTED_ACCOUNT_ID="<WORKLOAD-ACCOUNT-ID>" \
CLOUD_NAME="<cloud-name>" \
./scripts/validation/export-baseline.sh <dev|staging|prod>
```

Expected generated package:

```text
validation-results/<env>/baseline/<timestamp>/
├── summary.md
├── summary.json
├── validate-env.log
├── validate-networking.log
├── validate-vpc-endpoints.log
├── validate-logging.log
├── validate-security-workload.log
├── validate-kms.log
├── validate-backup.log
├── validate-sns.log
├── validate-sqs.log
├── validate-eventbridge.log
├── validate-lambda.log
├── validate-ssm.log
├── validate-compute.log
└── validate-iam.log
```

| Area | Script | Result | Evidence Log | Notes |
|---|---|---|---|---|
| Environment | `validate-env.sh` | `<PASS/FAIL/Not Run>` | `validate-env.log` | `<notes>` |
| Networking | `validate-networking.sh` | `<PASS/FAIL/Not Run>` | `validate-networking.log` | `<notes>` |
| VPC Endpoints | `validate-vpc-endpoints.sh` | `<PASS/FAIL/Not Run>` | `validate-vpc-endpoints.log` | `<notes>` |
| Logging | `validate-logging.sh` | `<PASS/FAIL/Not Run>` | `validate-logging.log` | `<notes>` |
| Workload Security | `validate-security-workload.sh` | `<PASS/FAIL/Not Run>` | `validate-security-workload.log` | `<notes>` |
| KMS | `validate-kms.sh` | `<PASS/FAIL/Not Run>` | `validate-kms.log` | `<notes>` |
| Backup | `validate-backup.sh` | `<PASS/FAIL/Not Run>` | `validate-backup.log` | `<notes>` |
| SNS | `validate-sns.sh` | `<PASS/FAIL/Not Run>` | `validate-sns.log` | `<notes>` |
| SQS | `validate-sqs.sh` | `<PASS/FAIL/Not Run>` | `validate-sqs.log` | `<notes>` |
| EventBridge | `validate-eventbridge.sh` | `<PASS/FAIL/Not Run>` | `validate-eventbridge.log` | `<notes>` |
| Lambda | `validate-lambda.sh` | `<PASS/FAIL/Not Run>` | `validate-lambda.log` | `<notes>` |
| SSM | `validate-ssm.sh` | `<PASS/FAIL/Not Run>` | `validate-ssm.log` | `<notes>` |
| Compute | `validate-compute.sh` | `<PASS/FAIL/Not Run>` | `validate-compute.log` | `<notes>` |
| IAM | `validate-iam.sh` | `<PASS/FAIL/Not Run>` | `validate-iam.log` | `<notes>` |

Current successful workload baseline exports contain 14 of 14 passing scripts. The report should use the generated package as the source of truth if the suite changes later.

---

## Control-Plane Validation Results

Use this section only when the validation scope is `control-plane`.

The current exporter consumes the consolidated Identity Center workload and security-operations JSON inputs used by the Terraform stack.

Generated by:

```bash
IDENTITY_CENTER_WORKLOADS='<JSON-WORKLOAD-CONFIGURATION-MAP>' \
IDENTITY_CENTER_SECOPS='<JSON-SECURITY-OPERATIONS-CONFIGURATION>' \
AWS_PROFILE="control-plane" \
AWS_REGION="<region>" \
EXPECTED_ACCOUNT_ID="<CONTROL-PLANE-ACCOUNT-ID>" \
EXPECTED_GITHUB_REPOSITORY="<GITHUB-OWNER>/<GITHUB-REPO>" \
REQUIRE_STATE_STACK_REMOTE=true \
CLOUD_NAME="<cloud-name>" \
./scripts/validation/export-control-plane.sh
```

Expected generated package:

```text
validation-results/control-plane/<timestamp>/
├── summary.md
├── summary.json
└── validate-control-plane.log
```

### Control-Plane Summary

| Area | Script | Result | Evidence Log | Notes |
|---|---|---|---|---|
| Control Plane | `validate-control-plane.sh` | `<PASS/FAIL/Not Run>` | `validate-control-plane.log` | `<notes>` |

### Control-Plane Settings

| Setting | Value |
|---|---|
| `CONTROL_PLANE_ENV_NAME` | `<control-plane>` |
| `REQUIRE_CONTROL_PLANE_GITHUB_OIDC` | `<true/false>` |
| `EXPECTED_GITHUB_REPOSITORY` | `<owner/repo/not-set>` |
| `CHECK_OPTIONAL_SECOPS_GROUPS` | `<true/false>` |
| `STRICT_IDENTITY_CENTER_ASSIGNMENTS` | `<true/false>` |
| `STRICT_ACCOUNT_OU_CHECKS` | `<true/false>` |
| `REQUIRE_STATE_STACK_REMOTE` | `<true/false>` |
| Workloads / NonProd / Prod / Security OU names | `<values>` |
| Dev / Staging / Prod account IDs | `<values>` |
| Security-Operations account ID | `<account-id>` |

### Control-Plane Coverage

| Area | Result | Evidence Log | Notes |
|---|---|---|---|
| Control-plane AWS identity and stack structure | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Control-plane state backend, native locking, and remote-state readability | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Control-plane state bucket and CMK protections | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Control-plane GitHub OIDC Plan/Apply roles and repository trust | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| AWS Organizations ALL-features mode and root/OU topology | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Dev, staging, prod, and security-operations account identity and OU placement | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Security Hub / GuardDuty trusted access and delegated-administrator registration | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Security Hub V2 `SECURITYHUB_POLICY` organization prerequisite | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |
| Identity Center instance, required groups, permission sets, and assignments | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-control-plane.log` | `<notes>` |

`STRICT_ACCOUNT_OU_CHECKS` and `STRICT_IDENTITY_CENTER_ASSIGNMENTS` default to `true` in the current validator, so required topology and assignment mismatches are blocking findings unless a run deliberately relaxes those settings.

---

## Security-Operations Validation Results

Use this section only when the validation scope is `security-operations`.

Generated by:

```bash
AWS_PROFILE="security-operations" \
AWS_REGION="<region>" \
EXPECTED_ACCOUNT_ID="<SECURITY-OPERATIONS-ACCOUNT-ID>" \
CLOUD_NAME="<cloud-name>" \
./scripts/validation/export-security-operations.sh
```

Expected generated package:

```text
validation-results/security-operations/security-services/<timestamp>/
├── summary.md
├── summary.json
└── validate-security-operations.log
```

### Security-Operations Summary

| Area | Script | Result | Evidence Log | Notes |
|---|---|---|---|---|
| Security Operations | `validate-security-operations.sh` | `<PASS/FAIL/Not Run>` | `validate-security-operations.log` | `<notes>` |

### Security-Operations Coverage

| Area | Result | Evidence Log | Notes |
|---|---|---|---|
| Security-operations AWS identity and applied security-services Terraform state | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-security-operations.log` | `<notes>` |
| Security Hub and GuardDuty trusted access / delegated administration dependencies | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-security-operations.log` | `<notes>` |
| Security Hub CSPM administrator state, finding aggregation, CENTRAL configuration, policies, and workload associations | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-security-operations.log` | `<notes>` |
| GuardDuty administrator detector, organization enrollment, protection plans, and Runtime Monitoring configuration | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-security-operations.log` | `<notes>` |
| Security Hub V2 administrator state, organization policy attachment, and effective workload policies | `<PASS/WARN/FAIL/Not Reviewed>` | `validate-security-operations.log` | `<notes>` |

The security-operations report intentionally does not replace full AWS Organizations topology validation or workload-local validation. Those responsibilities remain with the control-plane and workload evidence layers respectively.

---

## Validation Remaining Outside the Selected Report

The automated exporters are intentionally read-only and layer-specific. A check can therefore be outside one report while still being covered by another automated evidence workflow.

| Check | Evidence Path / Status | Evidence / Notes |
|---|---|---|
| Control-plane and Organizations topology validation | `Export Control Plane Evidence` / `<Complete/Outstanding/Not Applicable>` | `<notes>` |
| Security-operations centralized governance validation | `Export Security Operations Evidence` / `<Complete/Outstanding/Not Applicable>` | `<notes>` |
| Workload bootstrap validation | `Export Bootstrap Evidence` / `<Complete/Outstanding/Not Applicable>` | `<notes>` |
| Workload baseline validation | `Export Baseline Evidence` / `<Complete/Outstanding/Not Applicable>` | `<notes>` |
| IAM Identity Center end-user login and effective access testing | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Live EC2 isolation test | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Live EC2 rollback test | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Live IP enrichment test | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Tamper detection test | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Break-glass role assumption test | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |
| Destroy safety review | Manual / `<Not Started/In Progress/Complete/Not Applicable>` | `<notes>` |

---

## Warnings / Exceptions

Document warnings, expected deviations, skipped checks, or environment-specific exceptions.

| Item | Severity | Description | Disposition |
|---|---|---|---|
| `<item>` | `<Low/Medium/High>` | `<description>` | `<accepted/remediated/deferred/not applicable>` |

Examples of environment-specific exceptions may include:

- Backup resources not required when `effective_backup_enabled = false`
- Network Firewall not required when `effective_egress_mode = nat_only`
- Network Firewall and NAT Gateway not required when `effective_egress_mode = vpc_endpoints_only`
- AWS Config rules not required when Config is intentionally disabled by deployment profile
- Pending SNS email confirmation where subscriber approval is still required
- Optional Identity Center Analyst/Engineer groups intentionally disabled
- Workload CMK policy checks intentionally run with `STRICT_WORKLOAD_CMK_POLICY_CHECKS=false` during transitional validation
- State-stack remote validation intentionally run with `REQUIRE_STATE_STACK_REMOTE=false`, producing advisory findings instead of strict migration evidence
- A deliberately relaxed `STRICT_ACCOUNT_OU_CHECKS=false` or `STRICT_IDENTITY_CENTER_ASSIGNMENTS=false` run, which should be identified as weaker evidence than the default strict mode

---

## Evidence Files

Document the files included in the selected generated evidence package.

| File | Purpose |
|---|---|
| `summary.md` | Human-readable validation report for the selected scope |
| `summary.json` | Machine-readable validation summary for the selected scope |
| `<validate-*.log>` | Supporting validation log generated by the selected exporter |

---

## Limitations

This report validates deployed AWS control presence and selected configuration settings for the selected validation scope.

The validation scripts confirm the presence and configuration of selected AWS security controls, governance resources, state backend resources, GitHub OIDC resources, and supporting infrastructure at the time validation was run.

This report does not replace:

- A full SOC 2 audit
- A full ISO 27001 audit
- Control owner review
- Policy and procedure review
- Control design assessment
- Control operating effectiveness testing
- Formal audit evidence review
- Risk assessment
- Vendor risk review
- Incident response testing
- Business continuity or disaster recovery testing
- A complete Information Security Management System

---

## Sign-Off

| Role | Name | Date | Notes |
|---|---|---|---|
| Technical Reviewer | `<name>` | `<date>` | `<notes>` |
| Security Reviewer | `<name>` | `<date>` | `<notes>` |
| Client / Stakeholder | `<name>` | `<date>` | `<notes>` |